require 'rails_helper'

describe Admin::CreateProductForm do

  describe '.initialize' do
    it 'should setup correctly' do
      # params = FactoryGirl.attributes_for(:create_product_form)
      # product = Product.create
      # form = Admin::CreateProductForm.new(product, params)

      # puts '------------------------'
      # puts form.inspect
      # puts '------------------------'

      # expect(form.name).to eq(params[:name])
      # expect(form.brand).to eq(params[:brand])
      # expect(form.description).to eq(params[:description])
      # expect(form.sku).to eq(params[:sku])
      # expect(form.price).to eq(params[:price])
    end
  end

end
